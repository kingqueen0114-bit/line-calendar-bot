{
  "name": "line-calendar-bot",
  "version": "1.0.0",
  "description": "LINE Calendar & Tasks Bot for Google Cloud Run",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "deploy:cloudflare": "wrangler deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["line", "calendar", "tasks", "bot"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/firestore": "^7.3.0",
    "@google-cloud/storage": "^7.7.0",
    "canvas": "^3.2.1",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
